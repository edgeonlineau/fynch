dist
node_modules
package-lock.json
.claude
Platforms

# GTM template lives in its own repo, embedded here as a submodule.
gtm-template

# GTM container export — keep GTM's own format, not Prettier's.
docs/example-ga4-container.json
